ANTHROPIC_API_KEY=sk-ant-...
PORT=8792
LEADS_DIR=~/zx-leads
MODEL=claude-sonnet-4-6

# ALUNO_TOKEN — OBRIGATÓRIO em produção. Protege endpoints /api/* e /dashboard.
# Gere com:
#   python -c "import secrets; print(secrets.token_urlsafe(24))"
# Se vazio: INSECURE_DEV_MODE (loga warning a cada acesso, aceita tudo).
ALUNO_TOKEN=
